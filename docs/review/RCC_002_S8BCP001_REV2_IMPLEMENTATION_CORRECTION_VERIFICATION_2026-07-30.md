# RCC-002 S8BCP-001 Revision 2 Implementation Correction Verification

Date: 2026-07-30

Status: IMPLEMENTATION CANDIDATE - NOT CERTIFIED

## 1. Authorized scope

This candidate implements only the scope authorized by
`RCC_002_S8BCP001_REV2_CORRECTED_NORMATIVE_BUNDLE_CERTIFICATION_DECISION_2026-07-30.md`:

1. S0/S1 source coverage, Source Snapshot V1 identity, registered
   timestamp-unit normalization, Source Manifest 1.0.0, and source-row
   identity V2;
2. S1 `source_file_ordinal` and `original_record_index` materialization and
   unchanged propagation through S7;
3. S3-to-S7 `indicator_schema_ref` conformance repair;
4. downstream implementation patch-version and deterministic S7 identity
   updates.

S8 implementation and dataset publication remain prohibited.

## 2. Implemented controls

- Registered Binance Vision daily and monthly portable-name parsing.
- Period-only timestamp-unit selection; timestamp magnitude is never a
  branch input.
- Millisecond handling before 2025-01-01 and microsecond handling from
  2025-01-01.
- Microsecond remainder checks (`open mod 1000 = 0`,
  `close mod 1000 = 999`) and exact integer conversion.
- One-minute post-conversion invariants.
- Strict ZIP, single-member, member-name, UTF-8-no-BOM, LF, headerless,
  twelve-column, checksum, and coverage checks.
- Canonical Source Snapshot V1 preimage and
  `source:sha256:<lowercase digest>` identity.
- Canonical registered multi-file Source Manifest 1.0.0 materialization with
  identity/coverage reconciliation against its source-file descriptors.
- Fail-closed rejection of any Source Manifest whose stored `source_files`
  array differs from the canonically ordered Source Snapshot V1 array, using
  `RCC_SOURCE_FILE_ORDER_MISMATCH`.
- Collision-free
  `RCC002_S1_SOURCE_ROW_ID_V2:<source_snapshot_id>:<ordinal 8d>:<index 20d>`.
- Structured Source Row ID V2 contract errors, including
  `RCC_SOURCE_ROW_ID_WIDTH_OVERFLOW` and
  `RCC_SOURCE_RECORD_INDEX_NOT_ORIGINAL`.
- S1 boundary validation that every V2 row ID exactly encodes the row's stored
  source snapshot, canonical file ordinal, and original pre-sort record index.
- Fail-closed rejection of the registered `BINANCE_VISION` provider, including
  case variants and migrated legacy aliases, on the generic legacy-ingestion
  path. Registered Binance Vision input must use the strict archive scan,
  Source Snapshot V1, and Source Manifest 1.0.0 path.
- Exact S1 source coordinates propagated unchanged through S2-S7.
- Exact S3 `indicator_schema_ref` placement and S4-S7 preservation.

## 3. Mechanical verification

Executed in the isolated implementation-correction tree:

```text
python3 -m compileall -q rcc002 scripts/rcc002 tests/rcc002
PASS

python3 scripts/rcc002/verify_source_profile_implementation.py \
  RCC_002_BINANCE_PROVIDER_EVIDENCE_INPUT_2026-07-30.zip
PASS

python3 -m unittest discover -s tests/rcc002 -p 'test_*.py'
Ran 631 tests
OK
```

Provider-evidence result:

```text
archive_count=4
record_count=92160
source_row_id_unique_count=92160
2024 timestamp unit=MILLISECOND
2025 timestamp unit=MICROSECOND
S0/S1 normalization parity=PASS for every archive
```

All four `source_identity_golden.v1.json` negative cases are directly bound
to tests and their registered error codes. The exact literal
`rcc002.stage.s3-indicators/1.0.0` is independently asserted.

### 3.1 Checksum-ledger scope

The repository-root `SHA256SUMS` in this candidate is the certified
S8BCP-001 Revision 2 **normative-bundle ledger**. Its scope is specifications,
registries, schemas, normative fixtures, normative verification scripts, and
the associated normative review artifacts. It is intentionally not, and must
not be interpreted as, a complete checksum inventory of the implementation
source tree or implementation tests.

The ledger remains byte-identical to the certified normative input and is not
regenerated by this implementation correction. Complete implementation
integrity is established by the re-review package checksum ledger, the exact
baseline-to-candidate change inventory, controlled Git import, and the
post-import RCC-002/regression/artifact verification gates.

The full repository regression suite and repository-wide normative artifact
verification must be repeated after controlled import because the isolated
review tree does not contain the repository's unrelated `run_engine` package
or every historical certification artifact.

## 4. Deterministic implementation identities

```text
S4 component version=0.3.1
S5 component version=0.4.1
S6 component version=0.4.1
S7 component version=0.3.1

LABEL_SCHEMA_FINGERPRINT_SHA256=
6fb2bcdeae2070f054fcf298693382b668ed267017f4e0f72be87615a3827bce

SEMANTIC_BUILD_CONFIGURATION_SHA256=
8ca946ee9725e6f767ff498c7d41362181f52457de7f5494a48a4b32b0806905
```

These values include the corrected S6 input-field order consumed by S7.

## 5. Remaining gates

Before this candidate can be certified:

1. independent Scientific Consistency Review;
2. independent Architecture Integrity Review;
3. resolution of every blocking review finding;
4. controlled repository import;
5. full RCC-002 and repository regression suites;
6. repository-wide artifact verification;
7. corrected-implementation certification.

Only after those gates may the S8 input package be regenerated and subjected
to a repeated S8 readiness review.
