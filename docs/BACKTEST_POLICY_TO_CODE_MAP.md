# Backtest Policy-to-Code Map (SimTrader / Backtest Integrity)

Scope: engine backtest/simulation, fees, slippage, timestamp integrity, leakage prevention.
Rule: Mapping first. No refactor without explicit decision.

## Mapping Table

| Policy-Item             | Erwartung (konkret)                                                                                             | Code-Stelle (Datei:Zeile)                                                                                                                                                                                                                                                                                          | Status (OK/Gap) + Notiz                                                                                                                       |
| ----------------------- | --------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------- |
| BT-FEE-001 Fees applied | Fees werden **explizit** als **Roundtrip (Entry+Exit)** in % vom Return abgezogen; Konfiguration reproduzierbar | `engine/simtraderGS.py:16` (Doku: Return minus `fee_roundtrip_pct`); `engine/simtraderGS.py:172–179` (`_fee_roundtrip()` via ENV `SIMTRADERGS_FEE_ROUNDTRIP` oder Config); `engine/simtraderGS.py:213,218,223,234,275,280,285,296` (konsequentes `r = r - fee_rt`); `engine/simtraderGS.py:336` (Bindung `fee_rt`) | **OK** – Fees sind zentral, deterministisch, explizit; Default 0.0 (GS), produktionsnah via ENV erzwingbar; keine impliziten/vergessenen Fees |


| Policy-Item                 | Erwartung (konkret)                                                                                                                               | Code-Stelle (Datei:Zeile)                                                                                                                                                                                                                                                                                                                                                               | Status (OK/Gap) + Notiz                                                                                                                                                                                                |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| BT-EXE-001 Fill assumptions | Entry/Exit-Preisannahmen sind **explizit** definiert und deterministisch (kein implizites „best price“). Dokumentiert, ob same-bar oder next-bar. | `engine/simtraderGS.py:15` (Doku: Entry/Exit auf `close[j]`, next-bar optional erst in GS v2); `engine/simtraderGS.py:202` (short entry `entry_px = close[i]`); `engine/simtraderGS.py:208,231` (short return mit `close[j]`); `engine/simtraderGS.py:264` (long entry `entry_px = close[i]`); `engine/simtraderGS.py:270,293` (long return mit `close[j]`); TP/SL Parameter: `331–332` | **GAP (Realismus-Risiko, bewusst akzeptiert in GS)** – Same-bar/close-execution kann Optimismus erzeugen, wenn Signal auf derselben Bar generiert wird. Dokumentation ist klar; next-bar ist als v2-Option vorgesehen. |


| Policy-Item             | Erwartung (konkret)                                                                                                          | Code-Stelle (Datei:Zeile)                                                                                                                                                                                                                                                                   | Status (OK/Gap) + Notiz                                                                                                                                                                                                                      |
| ----------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| BT-LA-001 No look-ahead | Signale/Score müssen kausal sein (typisch: Signal bei i basiert auf Daten ≤ i-1 oder Entry erfolgt next-bar). Keine Leakage. | `_prep_signals()` liest Signals 1:1 aus CSV ohne Shift: `engine/simtraderGS.py` in `_prep_signals()` (Signals → `_SIG[col]=arr`); `score` wird auf gleichem Index gebildet: `_eval_core()` (`score += weight * z[col]`); Entry/Execution same-bar: `entry_px = close[i]` (siehe BT-EXE-001) | **GAP (Look-ahead/Leakage-Risiko, abhängig von Signal-CSV)** – simtraderGS erzwingt keine kausale Ausrichtung. Korrektheit hängt davon ab, dass die Signalspalten in der CSV bereits strikt kausal (z. B. um 1 Bar verzögert) gebaut wurden. |


| Policy-Item             | Erwartung (konkret)                                                                                                      | Code-Stelle (Datei:Zeile)                                                                                                                                                                                                                                                                                                                                                                       | Status (OK/Gap) + Notiz                                                                                                                                                                                                                                     |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| BT-LA-001 No look-ahead | Signale/Score müssen kausal sein (typisch: Signal bei i basiert auf Daten ≤ i-1 oder Execution next-bar). Keine Leakage. | Signal-Pipeline erzeugt `*_signal` same-bar ohne globalen Shift: `scripts/post_gs_h3_build_eth_signals_regime_gs_compat.py` (Signals: `df["rsi_signal"]=...`, `macd_signal`, `bollinger_signal`, `ma200_signal`, `stoch_signal`, `atr_signal`, `ema50_signal`, etc.); Engine nutzt score same-index und executed auf close[i] (siehe `engine/simtraderGS.py` Score-Build + `entry_px=close[i]`) | **GAP (Realismus / potential same-bar leakage)** – Der Stack (Signals same-bar + close-execution same-bar) ist nicht kausal abgesichert. Akzeptiert als GS v1, aber muss als GS v2 Kandidat geführt werden: *next-bar execution* oder *signal shift (i-1)*. |


| Policy-Item               | Erwartung (konkret)                                                                                         | Code-Stelle (Datei:Zeile)                                                                                                                                                                                                                                                                                                                                                      | Status (OK/Gap) + Notiz                                                                                       |
| ------------------------- | ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------- |
| BT-TS-001 Timestamp basis | Primärer Zeitstempel ist klar definiert (UTC), minute-aligned, monoton; Preflight prüft Monotonie im Window | Pipeline verlangt `timestamp_utc` und prüft Alignment + Monotonie: `scripts/post_gs_h3_build_eth_signals_regime_gs_compat.py:168` (required timestamp_utc), `174` (pd.to_datetime(..., utc=True)), `176` (minute-aligned), `179–180` (monotonic increasing). Zusätzlich Preflight: `tools/gs_input_preflight.py:81` (utc=True), `86–93` (check_monotonic), `179` (apply check) | **OK** – Zeitbasis ist explizit und wird aktiv validiert; schützt gegen Reihenfolgefehler und Timestamp-Drift |


| Policy-Item                | Erwartung (konkret)                                                                                          | Code-Stelle (Datei:Zeile)              | Status (OK/Gap) + Notiz |                                                                                                         |                                                                                                                                                 |
| -------------------------- | ------------------------------------------------------------------------------------------------------------ | -------------------------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| BT-SLIP-001 Slippage model | Slippage/Spread ist entweder explizit modelliert oder explizit = 0 dokumentiert (keine implizite Optimistik) | **Negativbeleg:** Suche nach `slippage | slip                    | spread`in`engine/`liefert keine Treffer;`engine/simtraderGS.py` enthält Fees, aber kein Slippage-Modell | **GAP (Realismus)** – Slippage/Spread aktuell nicht modelliert. Akzeptiert in GS v1; als GS v2 Kandidat führen (konservatives Default-Slippage) |


| Policy-Item                           | Erwartung (konkret)                                                                                                                   | Code-Stelle (Datei:Zeile)                                                                                                            | Status (OK/Gap) + Notiz                                                                                                                          |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| BT-LOG-001 Run metadata & determinism | Jeder Backtest-Run ist eindeutig rekonstruierbar (Run-ID, CSV-Pfad, ENV-Overrides, Fee, Flags) – entweder im Result oder als Manifest | `engine/simtraderGS.py` ENV-Overrides: SIMTRADERGS_PRICE_CSV (≈98), SIMTRADERGS_USE_FORWARD (≈103), SIMTRADERGS_FEE_ROUNDTRIP (≈173) | **GAP (Auditability)** – Determinismus vorhanden, aber **keine Run-Metadata-Persistenz**. GS v1 akzeptiert; GS v2: `run_manifest.json` empfehlen |



## Notes / Findings

### GS v2 candidate: run_manifest (no code change in GS v1)
- Motivation: close-exec + same-bar signals + no slippage + no persisted run-metadata => audit gaps
- Minimal artifact: results/GS/meta/run_manifest_<UTC>.json
- Contents: git commit hash, engine/simtraderGS.py sha256, price_csv path, rows/window, direction, fee_roundtrip, use_forward, enter_z/exit_z, tp/sl/max_hold, slippage_model (explicit 0), timestamp_col
- Rule: manifest is written by an external runner (tools/), not by simtraderGS.py
- Policy mapping: closes BT-LOG-001 partially (auditability) without touching GS engine

